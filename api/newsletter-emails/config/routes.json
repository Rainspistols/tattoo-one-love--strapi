{
  "routes": [
    {
      "method": "GET",
      "path": "/newsletter-emails",
      "handler": "newsletter-emails.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newsletter-emails/count",
      "handler": "newsletter-emails.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/newsletter-emails/:id",
      "handler": "newsletter-emails.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/newsletter-emails",
      "handler": "newsletter-emails.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/newsletter-emails/:id",
      "handler": "newsletter-emails.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/newsletter-emails/:id",
      "handler": "newsletter-emails.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
